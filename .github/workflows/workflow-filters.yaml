name: Workflow filters & Activities
on:
    push:
        branches:
            - main
    workflow_dispatch:
    pull_request:
jobs:
    filters_job:
        runs-on: ubuntu-latest
        steps:
            - name: "Running on various activites"
              run: "echo this workflow is running for the event type - ${{ github.event_name }}"